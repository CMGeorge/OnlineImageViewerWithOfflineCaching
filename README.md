TODO:

- cell and the presented view should share same image object. we need a wraper over to allow incapsulate the model and the other usefull informations needed 
- On device connection state changed to online, automatically fetch offline displayed items
- Add pull to refresh 
- Add translation support
